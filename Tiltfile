# -*- mode: Python -*-
# PocketScribe — Tilt dev stack
#
# Placeholder. Tilt config is not yet implemented — the project is currently
# at the "schema + skills" stage; there is no Go service to live-reload.
#
# When implemented, the stack will include:
#   - poktroll devnet (FilePlugin enabled)
#   - NATS JetStream
#   - PostgreSQL + TimescaleDB
#   - ps fileplugin sidecar + ps consumer <module>
#   - Hasura + PostgREST (downstream)
#   - Prometheus + Grafana + Loki + Promtail
#
# See ROADMAP.md (Phase 1 — Spike) for the work required to populate this.
print("PocketScribe Tilt config not yet implemented. See ROADMAP.md.")
fail("Tilt config not yet implemented.")
